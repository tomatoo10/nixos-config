Add instruction at agents.md saying that commits should have a descriptive title and a explanation about the changes, why and what changed.

Make sure our btrfs partitions and subvolumes are optiomal, shiro has some weird folder at root like @, @home and @nix, maybe i did something wrong.

Install prowllarr

Import Recyclarr Profiles now that the api keys are in place and tell the user how is the interval between each sync.

Pull changes from github, fixed some issues regarding dns.
